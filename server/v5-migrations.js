const db=require('./db');
const statements=[
`CREATE TABLE IF NOT EXISTS production_sources (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,type TEXT NOT NULL DEFAULT 'camera',url TEXT DEFAULT '',status TEXT NOT NULL DEFAULT 'offline',channel_id INTEGER,position INTEGER NOT NULL DEFAULT 0,notes TEXT DEFAULT '',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(channel_id) REFERENCES channels(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS rundown_items (id INTEGER PRIMARY KEY AUTOINCREMENT,programme_id INTEGER,position INTEGER NOT NULL DEFAULT 0,item_type TEXT NOT NULL DEFAULT 'story',title TEXT NOT NULL,duration_seconds INTEGER NOT NULL DEFAULT 30,source_url TEXT DEFAULT '',notes TEXT DEFAULT '',status TEXT NOT NULL DEFAULT 'ready',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(programme_id) REFERENCES programmes(id) ON DELETE CASCADE)`,
`CREATE TABLE IF NOT EXISTS graphics (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,type TEXT NOT NULL DEFAULT 'lower-third',content TEXT DEFAULT '',asset_url TEXT DEFAULT '',enabled INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
`CREATE TABLE IF NOT EXISTS studio_sessions (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'planned',channel_id INTEGER,programme_id INTEGER,director TEXT DEFAULT '',floor_manager TEXT DEFAULT '',notes TEXT DEFAULT '',started_at TEXT,ended_at TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(channel_id) REFERENCES channels(id) ON DELETE SET NULL,FOREIGN KEY(programme_id) REFERENCES programmes(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS control_events (id INTEGER PRIMARY KEY AUTOINCREMENT,channel_id INTEGER,event_type TEXT NOT NULL,title TEXT NOT NULL,details TEXT DEFAULT '',created_by INTEGER,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(channel_id) REFERENCES channels(id) ON DELETE CASCADE,FOREIGN KEY(created_by) REFERENCES users(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS master_playout (channel_id INTEGER PRIMARY KEY,mode TEXT NOT NULL DEFAULT 'scheduled',active_source TEXT DEFAULT '',backup_source TEXT DEFAULT '',emergency_source TEXT DEFAULT '',automation_enabled INTEGER NOT NULL DEFAULT 1,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(channel_id) REFERENCES channels(id) ON DELETE CASCADE)`
,`CREATE TABLE IF NOT EXISTS crew (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,role TEXT NOT NULL,department TEXT NOT NULL DEFAULT 'production',email TEXT DEFAULT '',phone TEXT DEFAULT '',status TEXT NOT NULL DEFAULT 'available',notes TEXT DEFAULT '',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
`CREATE TABLE IF NOT EXISTS crew_assignments (id INTEGER PRIMARY KEY AUTOINCREMENT,crew_id INTEGER NOT NULL,session_id INTEGER,programme_id INTEGER,role TEXT NOT NULL,shift_start TEXT,shift_end TEXT,status TEXT NOT NULL DEFAULT 'assigned',notes TEXT DEFAULT '',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(crew_id) REFERENCES crew(id) ON DELETE CASCADE,FOREIGN KEY(session_id) REFERENCES studio_sessions(id) ON DELETE CASCADE,FOREIGN KEY(programme_id) REFERENCES programmes(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS production_cues (id INTEGER PRIMARY KEY AUTOINCREMENT,session_id INTEGER,programme_id INTEGER,position INTEGER NOT NULL DEFAULT 0,cue_type TEXT NOT NULL DEFAULT 'camera',label TEXT NOT NULL,source_id INTEGER,graphic_id INTEGER,duration_seconds INTEGER NOT NULL DEFAULT 0,status TEXT NOT NULL DEFAULT 'standby',notes TEXT DEFAULT '',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(session_id) REFERENCES studio_sessions(id) ON DELETE CASCADE,FOREIGN KEY(programme_id) REFERENCES programmes(id) ON DELETE SET NULL,FOREIGN KEY(source_id) REFERENCES production_sources(id) ON DELETE SET NULL,FOREIGN KEY(graphic_id) REFERENCES graphics(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS audio_channels (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,type TEXT NOT NULL DEFAULT 'mic',source_url TEXT DEFAULT '',enabled INTEGER NOT NULL DEFAULT 1,mute INTEGER NOT NULL DEFAULT 0,gain REAL NOT NULL DEFAULT 0,pan REAL NOT NULL DEFAULT 0,notes TEXT DEFAULT '',updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
`CREATE TABLE IF NOT EXISTS comms_messages (id INTEGER PRIMARY KEY AUTOINCREMENT,from_user INTEGER,to_crew INTEGER,message TEXT NOT NULL,priority TEXT NOT NULL DEFAULT 'normal',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(from_user) REFERENCES users(id) ON DELETE SET NULL,FOREIGN KEY(to_crew) REFERENCES crew(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS broadcast_alerts (id INTEGER PRIMARY KEY AUTOINCREMENT,channel_id INTEGER,title TEXT NOT NULL,message TEXT NOT NULL,level TEXT NOT NULL DEFAULT 'info',active INTEGER NOT NULL DEFAULT 1,created_by INTEGER,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(channel_id) REFERENCES channels(id) ON DELETE CASCADE,FOREIGN KEY(created_by) REFERENCES users(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS graphic_state (channel_id INTEGER PRIMARY KEY,graphic_id INTEGER,active INTEGER NOT NULL DEFAULT 0,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(channel_id) REFERENCES channels(id) ON DELETE CASCADE,FOREIGN KEY(graphic_id) REFERENCES graphics(id) ON DELETE SET NULL)`]; for(const s of statements) db.exec(s);

// V6 broadcast automation: deterministic, server-clocked channel playout.
db.exec(`CREATE TABLE IF NOT EXISTS broadcast_items (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  channel_id INTEGER NOT NULL,
  title TEXT NOT NULL,
  source_type TEXT NOT NULL DEFAULT 'video',
  source_url TEXT NOT NULL,
  start_at TEXT NOT NULL,
  end_at TEXT NOT NULL,
  enabled INTEGER NOT NULL DEFAULT 1,
  created_by INTEGER,
  created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY(channel_id) REFERENCES channels(id) ON DELETE CASCADE,
  FOREIGN KEY(created_by) REFERENCES users(id) ON DELETE SET NULL
)`);
db.exec(`CREATE TABLE IF NOT EXISTS broadcast_override (
  channel_id INTEGER PRIMARY KEY,
  item_id INTEGER,
  title TEXT NOT NULL,
  source_type TEXT NOT NULL DEFAULT 'live',
  source_url TEXT NOT NULL,
  started_at TEXT NOT NULL,
  active INTEGER NOT NULL DEFAULT 1,
  created_by INTEGER,
  updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY(channel_id) REFERENCES channels(id) ON DELETE CASCADE,
  FOREIGN KEY(item_id) REFERENCES broadcast_items(id) ON DELETE SET NULL,
  FOREIGN KEY(created_by) REFERENCES users(id) ON DELETE SET NULL
)`);
